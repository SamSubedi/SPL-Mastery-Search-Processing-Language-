02. Query to identify potential security threats
================================================

sourcetype=access_* method=POST status=200
| rex field=_raw "password=(?<password>[^&]+)"
| eval password_length=length(password)
| where password_length >= 8
